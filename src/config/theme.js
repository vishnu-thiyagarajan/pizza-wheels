import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#df4a31",
      main: "#B22222",
      dark: "#92251b",
    },
    secondary: {
      light: "#6d6d6d",
      main: "#333333",
      dark: "#000000",
    },
  },
});

export default theme;
